global:
  slack_api_url: 'https://hooks.slack.com/services/INSERT_HOOK'

route:
  receiver: 'slack-notifications'

receivers:
- name: 'slack-notifications'
  slack_configs:
  - channel: '#INSERT_CHANNEL'
